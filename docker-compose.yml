version: '3.0'
services:
  db:
    image: postgres
    restart: always
    env_file:
      - ./.env  
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
  app:
    image: leaguex-app
    ports: 
        - 4500:4000
    env_file:
      - ./.env  








